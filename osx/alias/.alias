source $HOME/.dotfiles/osx/alias/.bash_functions

# SYSTEM
alias cl='clear'
alias reload="exec ${SHELL} -l"
alias r="exec ${SHELL} -l"
alias theme='bash -c  "$(curl -sLo- https://git.io/vQgMr)"'
alias x='exit'

# NETWORK PRIORITY
alias isp='curl ipinfo.io | grep org'

# NODE_MODULES
alias lsnodemodules='sudo find . -name 'node_modules' -type d -prune'
alias rmnodemodules='find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +'

# GIT
alias gll="git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
alias gsl="git stash list"

# NPM
alias npmg='npm list -g --depth 0'

# UTILS
alias dotfiles='code ~/.dotfiles'
alias dl='youtube-dl'

# K8s
alias k='kubectl'
alias mk='minikube'

# DOCKER
alias dpa='docker ps -a'
alias dps='docker ps'

## WORK
alias idm='cd ~/Work/idm'
alias dms='cd ~/Work/dms'
alias frankie='cd ~/OpenSource/Frankie'
alias dt='cd ~/Work/deployer'
alias pv='cd ~/Work/package-viewer'
alias she='cd ~/Work/shearer'
alias ss='cd ~/Work˜store-screen'

## PERSONAL
alias ts='cd ~/SideProjects/twitter-scheduler'
alias wcc='cd ~/SideProjects/we-craft-code'
alias fp='cd ~/SideProjects/famlypad'
